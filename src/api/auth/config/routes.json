{
    "routes": [
      {
        "method": "POST",
        "path": "/auth/refresh",  
        "handler": "auth.refresh",
        "config": {
          "policies": []
        }
      }
    ]
  }